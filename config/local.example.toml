# Copy to ~/.agentic-workflows/config.toml and edit locally.
# Windows example: projects_root = "C:/development/projects"
projects_root = "~/projects"

[linear]
# Set your personal API key here, in the local copy only.
api_key = ""

[agent]
kind = "codex"
# Use a model ID accepted by the selected agent and account.
model = "gpt-6-astra"
# Workflow mode means Codex reasoning effort or Pi thinking level.
mode = "high"

# Optional Codex-only override for a repository you explicitly trust. The value
# names $CODEX_HOME/agentic-workflows-trusted.config.toml; see the README.
# [codex.repositories."agentic-workflows"]
# profile = "agentic-workflows-trusted"
