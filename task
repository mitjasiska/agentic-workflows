#!/usr/bin/env python3.12
"""Repository-local entry point; also works through a symlink on PATH."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from task_start.cli import main

if __name__ == "__main__":
    sys.exit(main())
